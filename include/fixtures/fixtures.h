#ifndef CRAWLER_DETECT_FIXTURES_H
#define CRAWLER_DETECT_FIXTURES_H

#include <stddef.h>

extern const char *const CRAWLER_DETECT_CRAWLERS[];
extern const size_t CRAWLER_DETECT_CRAWLERS_COUNT;

extern const char *const CRAWLER_DETECT_EXCLUSIONS[];
extern const size_t CRAWLER_DETECT_EXCLUSIONS_COUNT;

#endif
